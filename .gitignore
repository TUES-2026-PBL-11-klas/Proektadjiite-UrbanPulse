node_modules/
.env
.env.local
dist
uploads


# Next.js build output
.next/
frontend/.next/

# Frontend dependencies
frontend/node_modules/

plan.md

# Terraform
terraform/.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
terraform.tfvars

# Kubernetes — never commit a secret.yaml with real values
# The committed template is k8s/secret.yaml.example
k8s/secret.yaml